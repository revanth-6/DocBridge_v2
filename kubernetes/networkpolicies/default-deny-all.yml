apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: default-deny-all
  namespace: docbridge
spec:
  podSelector: {}
  policyTypes:
    - Ingress
    - Egress
# Empty ingress and egress lists mean deny all traffic
