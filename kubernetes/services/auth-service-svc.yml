apiVersion: v1
kind: Service
metadata:
  name: auth-service-svc
  namespace: docbridge
spec:
  type: ClusterIP
  ports:
    - port: 3001
      targetPort: 3001
      protocol: TCP
  selector:
    app: auth-service
