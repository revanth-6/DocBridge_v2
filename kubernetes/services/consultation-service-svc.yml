apiVersion: v1
kind: Service
metadata:
  name: consultation-service-svc
  namespace: docbridge
spec:
  type: ClusterIP
  ports:
    - port: 3002
      targetPort: 3002
      protocol: TCP
  selector:
    app: consultation-service
