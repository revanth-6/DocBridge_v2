apiVersion: v1
kind: Service
metadata:
  name: health-summary-service-svc
  namespace: docbridge
spec:
  type: ClusterIP
  ports:
    - port: 3008
      targetPort: 3008
      protocol: TCP
  selector:
    app: health-summary-service
