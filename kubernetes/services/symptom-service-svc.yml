apiVersion: v1
kind: Service
metadata:
  name: symptom-service-svc
  namespace: docbridge
spec:
  type: ClusterIP
  ports:
    - port: 3006
      targetPort: 3006
      protocol: TCP
  selector:
    app: symptom-service
